// Inventory.qml
import QtQuick

Rectangle {
    id: inventory
    width: 300
    height: 400
    color: "black"
    visible: map.player.inventory.visible
    // Obsah inventáře (seznam položek, tlačítka atd.)

}
